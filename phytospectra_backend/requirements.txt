# Web API
fastapi
uvicorn[standard]
pydantic-settings
python-multipart

# Auth & HTTP
httpx==0.27.0
python-jose[cryptography]==3.3.0
PyJWT

# Database — supabase 2.10+ needs httpx 0.27+ and websockets 14+
websockets>=14.0
supabase>=2.10.0
# File watcher (core/watcher.py)
watchdog

# Image / geo processing
numpy
Pillow
exifread
matplotlib

# ML inference (CPU wheels — use GPU build if you have CUDA)
torch
torchvision
transformers

# Simulation script only (optional)
requests
